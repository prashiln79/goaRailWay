import { RailwayRoute } from '../types/RailwayRoute';

/**
 * Railway route polylines are loaded dynamically from Firebase Firestore.
 * Master seed dataset is preserved in `scripts/seedData/routes.ts`.
 */
export const RAILWAY_ROUTES: RailwayRoute[] = [];
